<%= form_for(@wine_allergy) do |f| %>
  <% if @wine_allergy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wine_allergy.errors.count, "error") %> prohibited this wine_allergy from being saved:</h2>

      <ul>
      <% @wine_allergy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
