<%= form_for(@maturation_type) do |f| %>
  <% if @maturation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maturation_type.errors.count, "error") %> prohibited this maturation_type from being saved:</h2>

      <ul>
      <% @maturation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
