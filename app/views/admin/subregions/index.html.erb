<div class="panel panel-default">
  <div class="panel-heading">
    <div class="row">
      <div class="col-md-6">
        <h3 class="panel-title">Listing Subregions</h3>
      </div>
      <div class="col-md-6">
        <%= will_paginate @subregions, :class => 'vyne_pagination' %>
      </div>
    </div>
  </div>

  <table class="table table-bordered">

    <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Region</th>
      <th></th>
    </tr>
    </thead>

    <tbody>
    <% @subregions.each do |subregion| %>
        <tr>
          <td><%= subregion.id %></td>
          <td><%= subregion.name %></td>
          <td><%= subregion.region.name %></td>
          <td class="button-wrapper">
            <%= link_to admin_subregion_path(subregion), class: 'btn btn-default' do %>
                <i class="glyphicon glyphicon-search"></i>
            <% end %>
          </td>
        </tr>
    <% end %>
    </tbody>
  </table>

  <ul class="list-group">
    <li class="list-group-item">
      <%= link_to upload_admin_subregions_path, class: 'btn btn-default' do %>
          <i class="glyphicon glyphicon-cloud-upload" aria-hidden="true"></i> Upload
      <% end %>
      <%= link_to 'New Subregion', new_admin_subregion_path, :class => 'btn btn-primary' %>
    </li>
  </ul>
</div>
