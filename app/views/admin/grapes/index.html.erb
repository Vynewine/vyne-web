<%= render :partial => "layouts/winemenu" %>

<h1>Listing Grapes</h1>
<%= link_to 'Upload', controller: 'grapes', action: 'upload' %><br/><br/><br/>
<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @grapes.each do |grape| %>
      <tr>
        <td><%= grape.id %></td>
        <td><%= grape.name %></td>
        <td class="button-wrapper">
          <%= link_to admin_grape_path(grape) do %>
            <i class="flaticon stroke zoom-1"></i>
          <% end %>
        </td>
        <td class="button-wrapper">
          <%= link_to edit_admin_grape_path(grape) do %>
            <i class="flaticon stroke pencil-2"></i>
          <% end %>
        </td>
        <td class="button-wrapper">
          <%= link_to admin_grape_path(grape), method: :delete, data: { confirm: 'Are you sure?' } do %>
            <i class="flaticon stroke trash-2"></i>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Grape', new_admin_grape_path %>
