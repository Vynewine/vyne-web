<%= form_for([:admin, @vinification]) do |f| %>
  <% if @vinification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vinification.errors.count, "error") %> prohibited this vinification from being saved:</h2>

      <ul>
      <% @vinification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      <%= f.label :method %><br>
      <%= f.text_field :method %>
    </div>
    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
