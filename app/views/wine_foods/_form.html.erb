<%= form_for(@wine_food) do |f| %>
  <% if @wine_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wine_food.errors.count, "error") %> prohibited this wine_food from being saved:</h2>

      <ul>
      <% @wine_food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
