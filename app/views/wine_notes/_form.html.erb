<%= form_for(@wine_note) do |f| %>
  <% if @wine_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wine_note.errors.count, "error") %> prohibited this wine_note from being saved:</h2>

      <ul>
      <% @wine_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
