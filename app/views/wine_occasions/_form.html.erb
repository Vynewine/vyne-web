<%= form_for(@wine_occasion) do |f| %>
  <% if @wine_occasion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wine_occasion.errors.count, "error") %> prohibited this wine_occasion from being saved:</h2>

      <ul>
      <% @wine_occasion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
