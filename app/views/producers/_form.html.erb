<%= form_for(@producer) do |f| %>
  <% if @producer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@producer.errors.count, "error") %> prohibited this producer from being saved:</h2>

      <ul>
      <% @producer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :country %><br>
    <%= select_tag :country,
        options_from_collection_for_select(@countries, "id", "name", :selected => @producer.country.id),
        :prompt => "Choose a country"
    %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
