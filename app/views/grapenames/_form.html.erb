<%= form_for(@grapename) do |f| %>
  <% if @grapename.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grapename.errors.count, "error") %> prohibited this grapename from being saved:</h2>

      <ul>
      <% @grapename.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
