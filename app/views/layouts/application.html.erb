<!DOCTYPE html>
<html>
<head>
  <title>Vynz</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0 " />
  <meta charset="utf-8">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="wrapper"<% if params[:controller] == 'home' %> class="home-page"<% end %>>
    <div class="w1">
      <header id="header">
        <div class="header-holder">
          <strong class="logo">
            <a href="#">
              <span data-picture data-alt="image description">
                <span data-src="logo.png" ></span>
                <span data-src="logo-2x.png"
                data-media="(-webkit-min-device-pixel-ratio:1.5), (min-resolution:144dpi)" ></span>
                <noscript>
                  <img src="logo.png" width="75" height="13" alt="image description" >
                </noscript>
              </span>
            </a>
          </strong>
          <a href="#" class="opener"><span></span></a>
<% if user_signed_in? %>
          <%= link_to "sign out",
          destroy_user_session_path,
          :method => :delete,
          :class => "login-link"
          %>
<% if current_user.has_role?(:supplier) || current_user.has_role?(:admin) || current_user.has_role?(:superadmin) %>
          <%= link_to 'Admin',
          producers_path,
          :class => "login-link"
          %>
<% end %>
<% else %>
          <%= link_to "Sign up",
          new_user_registration_path,
          :class => "login-link"
          %><%= link_to "sign in",
          new_user_session_path,
          :class => "login-link"
          %>
<% end %>
        </div>
      </header>
      <div id="main">
<%= yield %>
      </div>
    </div>
  </div>
</body>
</html>
